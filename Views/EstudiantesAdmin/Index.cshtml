@model List<SistemaMatriculaURA.Models.Estudiante>

@{
    ViewData["Title"] = "Gestión de Estudiantes";
}

<div class="container mt-4">
    <h1 class="mb-3">Gestión de Estudiantes</h1>

    <a asp-controller="EstudiantesAdmin" asp-action="Create" class="btn btn-primary mb-3">+ Registrar Estudiante</a>

    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success">@TempData["Success"]</div>
    }

    <div class="ura-box p-3">
        <table class="table table-hover">
            <thead>
                <tr>
                    <th>Nombre</th>
                    <th>Correo</th>
                    <th>Carrera</th>
                    <th class="text-end">Acciones</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var e in Model)
                {
                    <tr>
                        <td>@e.Nombre</td>
                        <td>@e.Correo</td>
                        <td>@e.Carrera?.Nombre</td>

                        <td class="text-end">
                            <a asp-controller="EstudiantesAdmin" asp-action="Edit" asp-route-id="@e.Id" class="btn btn-warning btn-sm">Editar</a>

                            <a asp-controller="EstudiantesAdmin" asp-action="Details" asp-route-id="@e.Id" class="btn btn-info btn-sm">Detalles</a>

                            <a asp-controller="EstudiantesAdmin" asp-action="Delete" asp-route-id="@e.Id" class="btn btn-danger btn-sm">Eliminar</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
